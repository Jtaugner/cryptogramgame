# Migration Progress Log

Tracks subtask outcomes for the admin quote-management tool plan in PLAN.md.

[2026-04-28T16:46:07Z] Subtask S1 — PASS — agent: Explore — report at admin-research.md (551 lines). Audit surfaced multiple per-language divergences; halting at S2 per PLAN.md stop condition ("S1 audit reveals a divergence ... that can't be unified without changing algorithm behavior") to get user decisions before unifying the generator.
[2026-04-28T16:46:07Z] Decisions from user recorded in PLAN.md "Decisions from S1 review" section. S2 spec updated accordingly.
[2026-04-28T17:05:00Z] Subtask S2 — developer agent reported done; could not self-verify (Bash denied). Created creatingLevels/{generator,dataForLanguages,configForLanguages}.cjs and rewrote createLevel.cjs. Orchestrator-side verification: pure-module require checks PASS. CLI run pending. Cleaned 4 unused-identifier warnings flagged by IDE diagnostics. Pre-existing project issues (broken `lint` config using --ext on flat eslint, broken `build:yandex` due to esbuild arm64/x64 mismatch in node_modules) are NOT caused by S2 changes.
[2026-04-28T19:25:00Z] Subtask S2 — PASS. Verifications run: (a) `node -e "require('./creatingLevels/{generator,dataForLanguages,configForLanguages}.cjs')"` all produce zero output (pure modules confirmed); (b) full CLI run with language='de' (levelTarget=null skip path) exits 0 in <1s, prints "skipping levels generation" and runs daily handling without errors; (c) `git diff --stat` confirms only createLevel.cjs modified (3 new cjs files + planning files untracked), no unintended writes to src/ or to ru/en legacy scripts. Could NOT verify: full language='en' end-to-end run (the loop `while(state.allLevels.length !== untill)` is from the original main verbatim and only converges in append direction — testing target=10 against existing 450 levels caused infinite loop, which is original behavior, not a regression). `npm run lint` and `npm run build:yandex` are broken at HEAD (pre-existing, unrelated to S2).
[2026-04-28T20:00:00Z] Subtask S3 — FAIL (attempt 1) — agent: developer. Initial implementation passed the 6 spec checks (--lang flag parsing, require-purity, deprecation comments, --lang de skip path) but had a real bug: `--lang ru` crashed with `Cannot find module './ru/output.js'` because RU's phrase corpus is `newCitats.js` (export `phrases`) and its daily file uses named export `dailyLevels`, while all other langs use `output.js` + `levelsData` + default export.
[2026-04-29T03:30:00Z] S6 enhancement — Bulk Add many + omit empty desc — PASS. (1) addMany checkbox in QuoteEditor (create mode, source/levels/locations only). When checked: text textarea grows to 14 rows; on Save text splits by /\n{3,}/, sequential POSTs share name+type+desc-if-set; inline progress "Saving N/M…"; on partial failure surfaces "Error after N/M". (2) Empty desc no longer stored: regenerateLevel only sets desc if non-empty string; saveSourceQuotes builds entry as {text, name, desc?, type} (desc key dropped if empty); POST/PUT validation now allows desc=undefined (still 400 on non-string). Smoke 6/6: A POST no desc → no desc field in regenerated and source; A2 disk confirms; B POST desc:"" → still no desc; C POST desc:"X" → desc preserved; D missing text → 400; E desc=number → 400. tsc clean. Side note: user did manual Add Location testing between turns — locationLevels.js and locations.json reformatted to 2-space indent (harmless).

[2026-04-29T03:00:00Z] S6 enhancement — Add Location button — PASS. New endpoints: GET /api/location-bucket/translations?key=YYYY-MM (returns {} for non-existent keys, 400 on bad format); POST /api/location-bucket {lang, key, translations} adds new bucket [] to src/levels/{lang}/locationLevels.js AND merges translations into src/locales/locations.json["location{key}"]. 409 on bucket conflict, 400 on bad key/lang/no translations. Frontend: blue "Add location" button visible in mode=locations only; modal with ID input (default = next month after last bucket, 0-indexed; if empty file uses current UTC year-month) and 6 translation inputs (RU/EN/FR/ES/IT/DE) with debounced (300ms) prefill from existing translations on ID change. After save: closes modal, refreshes locations dropdown, selects new bucket. Smoke 9/9: all CRUD + edge cases work; H test created 2099-05 bucket and translations, verified on disk, restored via git checkout. tsc clean.

[2026-04-29T02:30:00Z] S6 enhancement — excludedNames in configForLanguages — PASS. Added per-lang `excludedNames: string[]` field to configForLanguages.cjs. ru: ['Теория большого взрыва', 'Сверхъестественное', 'Доктор Хаус'] (porting legacy `if(phrase.name === '...') continue` from creatingLevels/ru/createLevel.cjs lines 242-244). Other langs: []. Both filter sites updated: unified CLI runCli filter (creatingLevels/createLevel.cjs) and admin /api/generate-levels filter (admin/server.cjs). Verified: would skip 2399 ru source phrases (800 Теория большого взрыва + 800 Доктор Хаус + 799 Сверхъестественное) of 15127 total.

[2026-04-29T02:00:00Z] S6 enhancement — Levels sort toggle — PASS. Server: GET /api/quotes accepts ?sort=asc|desc (default asc), reverses candidates BEFORE pagination when desc; only applies in mode=levels (silently ignored otherwise). Frontend: gray Sort toggle button next to mode select, visible only in mode=levels, persisted to localStorage as admin.levelsSort, included in reset effect deps so list refreshes on toggle. Smoke 6/6: asc 0,1,2; desc 4099,4098,4097 (total now 4100 because user clicked Generate 100); search+desc gives descending filtered indexes; sort silently ignored in source/locations modes; pagination desc continuous (page0 ends 4095, page1 starts 4094). tsc clean. Note: src/levels/ru/allLevels.js now shows +87 net lines vs HEAD due to user's intentional "Generate 100 levels" click — not auto-reverted.

[2026-04-29T01:30:00Z] S6 enhancement — Generate 100 levels button — PASS. New endpoint POST /api/generate-levels?lang=&count= (1..1000, default 100) reuses generator's addWordsToLevelsUntill with same params as unified CLI (50, 270, 3, 0.05). Filtering via configForLanguages, ё→е normalization for ru, shuffleArray. Returns {ok, before, after, added, requested}. 422 if filtered.length<count (theoretical — for count≤1000 doesn't fire on existing langs). Frontend: orange button "Generate 100 levels" (#f97316) right side of top bar via `marginLeft: 'auto'`, visible only when mode='levels'. Confirms before run, disables and shows "Generating…" while in flight (10–60s on ru). On success: alert "Added N (before → after)" and refresh list. Smoke 5/5: 400 on missing/invalid lang, 400 on count=0/10000, validates from text. NOT exercised the actual 100-level generation — would mutate disk and take 30+ seconds. tsc clean. NOTE: pre-test had stale `M src/levels/ru/allLevels.js` from prior smoke (formatting drift); restored via git checkout HEAD.

[2026-04-29T01:00:00Z] S6 enhancement — locations rework — PASS. Switched locations source/target from creatingLevels/ru/locationCitats.js (legacy, only "Евгений Онегин") to src/levels/{lang}/locationLevels.js (the actual game data, month-bucketed). Now full-sync: editing in admin updates the live game file directly. /api/locations returns LocationDescriptor[] {key, label, count} sorted by month-key, label format "YYYY-MM (top-name-in-bucket)". Locations available for ANY lang (gated on bucket count not lang); en/de/es/fr/it currently have empty {} files so dropdown shows 0 buckets. New endpoint paths: /api/location/:lang/:key/:index and POST /api/location/:lang/:key. Frontend: yellow "source-only" warning removed (no longer applies); cryptogram preview now shown for locations; small read-only "Bucket: 2026-03" label above editor form. Search aggregator now iterates all buckets across any lang (not just ru). Smoke 10/10: 6 ru buckets correctly labeled (Ну погоди / Уильям Воган / Джоуи Адамс / Мастер и Маргарита / Пушкин / Онегин); en empty=0; bucket size right (30 for 2026-03); Маргарита search hits 2 location entries in bucket 2026-01; 400/404 paths correct. tsc clean.

[2026-04-29T00:30:00Z] S6 enhancement — global search across all modes — PASS. New endpoint GET /api/search?lang&q&page&pageSize aggregates from levels, daily, locations (ru only), source, names with that fixed grouping order. Returns SearchItem union with mode tag per item; counts breakdown for the UI summary line. Frontend: when search input non-empty, list switches to global aggregation, mode dropdown becomes informational, "Add new" disabled (ambiguous), Mode badge column shown using MODE_COLORS palette (gray/green/cyan/amber/purple). When search cleared, list reverts to mode-driven view. App.tsx now passes editorTarget.mode to QuoteEditor (instead of sidebar mode), so clicking a search result opens the right editor variant. Smoke 7/7: empty q→400; Эйнштейн total=14 (2 levels+8 source+4 names); Афелий→daily=1; Онегин→17 (mostly source); Хайям→15 (6 levels+8 src+1 name); en/Mandela→1 name; pagination 973-result жизнь page0/page1 disjoint. tsc clean. UX note flagged to user: substring match scope is text-only for levels/source/daily/locations and name+desc for names — en/Mandela doesn't surface the level entry whose name="Nelson Mandela" because the level TEXT lacks "Mandela". User to decide whether to broaden levels/etc. search to name+desc.

[2026-04-29T00:00:00Z] S6 enhancement — type colors + names mode — PASS. (1) TYPE_COLORS palette in types.ts: quotes/poems/aphorisms/music/cinema/science get distinct light-bg/dark-text combos (blue/purple/green/pink/amber/teal); applied to type badges in QuoteList; mode-encoding badges (Daily/Location) keep their existing colors. (2) New mode 'names' for editing src/levels/{lang}/allNames.js across all langs. Server: GET /api/names (paginated, search on name+desc), GET/PUT/POST/DELETE /api/name/:lang/:name with URL-encoded keys. POST is the only path to add new name; PUT only updates existing desc; rename intentionally NOT supported (would cascade to allLevels.name references). saveAllNames serializes as `export default {\n  "key": "val",\n  ...\n};\n` with double-quoted keys (ru's existing single-quotes get normalized on first save). Frontend: 'Names' option in mode <select> (no lang gating). Editor variant for names: name input read-only on edit, free on create; desc textarea; no cryptogram preview. Smoke 8/8: ru 324 entries, en 415, search "пушкин" returns 24 matches (matches name and desc); 404/400 paths correct. tsc clean. Disk clean.

[2026-04-28T23:30:00Z] S6 enhancement — daily + locations modes, mode default=levels — PASS. Replaced onlyLevels boolean with mode enum: source|levels|daily|locations. Default `levels` (persisted to localStorage as admin.mode, migrating from legacy admin.onlyLevels). Server: GET /api/locations?lang returns distinct authors from locationCitats.js (RU only, [] elsewhere). New endpoints: GET/PUT/POST/DELETE /api/daily/:lang/:index (full source+target sync, target uses synthesized name=langData.definitions, desc='', type='science'); GET/PUT/POST/DELETE /api/location/:lang/:index (RU-only, source-only — does NOT touch src/levels/ru/locationLevels.js because that target is curated by month-bucket). Frontend: mode <select> in top bar with Locations option disabled for non-ru; location <select> shown when mode=locations; QuoteList shows cyan Daily #N badge for daily, yellow Location: <author> badge for locations; QuoteEditor has 3 variants — quote (source/levels), daily (text-only), location (text + location, permanent yellow warning "Editing source only. Regenerate target via CLI"). Smoke 9/9: default mode=levels, daily ru/en, locations endpoint ru/en, locations 400 cases, empty-body PUT 400. tsc clean. KNOWN DIVERGENCE: regenerated daily entries get type='science' (matches main/unified CLI) while existing dailyLevels.js entries are type='quotes' (legacy ru CLI). After admin edits, neighbors will mix. Flagged for user.

[2026-04-28T22:30:00Z] S6 enhancement — onlyLevels filter + infinite scroll — PASS. agent: developer + orchestrator-side smoke. Server: GET /api/quotes accepts onlyLevels=1, returns mode:'source'|'levels'; in levels mode iterates allLevels.js in order, resolves indexInFile via normalized-text→source-index map (first occurrence wins; orphans get indexInFile:null). Frontend: PAGE_SIZE=100, removed Prev/Next pagination, added IntersectionObserver-based infinite scroll with sentinel + 200px rootMargin, level-mode rows show green "Level #N" badge + dim "src #M" label, orphan levels are non-clickable with red hint. onlyLevels checkbox in top bar persisted to localStorage. Smoke: A source mode levelIndex:null ✓; B levels mode ordered ✓; C total=4000 (allLevels.length) ✓; D type=poems filtered=1125 ✓; E search="жизнь" filtered=255 ✓; F UI 200+title ✓; pagination strictly consecutive (0-9, 10-19, 20-29) ✓; tsc 0 errors; no disk mutations from read-only smoke. NOTE: prior tests left stale mutations in creatingLevels/ru/newCitats.js and src/levels/ru/allLevels.js — restored via git checkout before this smoke; final disk only has the 4 expected S2/S3/S4 modifications.

[2026-04-28T22:00:00Z] S5/S6 fix bundle — PASS. User reported: edited quotes were appended to end of allLevels.js, breaking level numbering. Three issues fixed: (1) PUT no longer push'es regenerated entry when oldText not found in allLevels — only updates source, returns matchedInAllLevels:false; (2) admin's normalizeText now applies ё→е when configForLanguages[lang].normalizeYo (mirrors legacy generator preprocessing), eliminating false-negative findIndex on ru entries with ё; (3) regenerateLevel applies ё→е BEFORE calling generator.createLevel, so stored allLevels.text matches the normalized form GETs expect (round-trip stability). UX: QuoteEditor.tsx now shows yellow warning when matchedInAllLevels:false. Smoke verified: Test1 (non-level PUT) — 4003→4003 lines unchanged; Test2 (ё-level PUT round-trip) — GET matched:true → PUT matched:true → GET-again matched:true (hiddenIndexes survive); tsc clean. Files restored via git checkout post-test.

[2026-04-28T21:30:00Z] Subtask S6 — PASS at code & build level — agent: developer (implementation) + orchestrator (smoke + tsc). Created 970 lines across admin/src/{App,QuoteList,QuoteEditor,CryptogramPreview}.tsx, api.ts, types.ts, index.css. App.tsx: top-bar lang/search-debounced/type/Add-new + paginated list. QuoteEditor: modal with text/name/desc/type, cryptogram preview from saved hiddenIndexes, Save (PUT/POST)/Delete-with-confirm/Cancel. AbortController on all fetches; refresh-counter pattern after save/delete. No new npm deps added. Smoke verified: `npx tsc -p . --noEmit` clean (exit 0); curl http://localhost:3070/ returns 200 with title/root/entry markers; Vite transforms .tsx live; API still routes through admin server. NOT verified: actual in-browser interaction (open quote, edit, save, see list reflect change) — orchestrator cannot drive a browser. Recommended: user does a manual golden-path test before S7.

[2026-04-28T21:00:00Z] Subtask S5 — PASS — agent: developer (implementation) + orchestrator (smoke). admin/server.cjs grew from 38 lines (S4 stub) to 457 lines. 7 endpoints registered: GET /api/health, /api/languages, /api/quotes, /api/quote/:lang/:source/:index; PUT/POST/DELETE for quote mutations. All 8 smoke checks green: A languages list returns 6 langs; B paginated ru list shows total=14836; C single-quote read works (matchedInAllLevels=false on the test entry, expected since only 4000/14836 phrases were ever selected by generator); D bad lang→400; E out-of-range index→404; F wrong source filename→400; G PUT empty body→400; H POST empty body→400. `git diff --stat HEAD` post-test unchanged from pre-S5 (no accidental mutations during read-only smoke). ESM file parsing uses fs.readFile + Function constructor (sidesteps require-cache concerns); not the fastest for 17k-line newCitats.js but acceptable for localhost. PUT/POST/DELETE were NOT exercised with valid bodies (per spec — would mutate user data); they are present and reject empty bodies correctly.

[2026-04-28T20:30:00Z] Subtask S4 — PASS — agent: developer (scaffold creation) + orchestrator (npm install + smoke). Created admin/ with package.json, vite.config.ts, tsconfig{,node}.json, index.html, src/{main,App}.tsx, server.cjs (Express + Vite middleware via dynamic import), .gitignore. Root package.json got exactly one added line: "admin": "cd admin && npm run dev". `npm install` in admin/ added 154 packages (16s). Smoke test: `PORT=3050 node server.cjs` → "Admin running on http://localhost:3050"; `curl /api/health` → `{"ok":true,"ts":...}`; `curl /` → index.html with Vite HMR scripts injected; server killed cleanly. Note: 2 moderate npm-audit warnings in admin deps (transitive); not blocking. Root build:yandex was NOT verified (pre-existing esbuild arm64/x64 mismatch unrelated to S4) — admin is structurally isolated (own node_modules, own vite config, only +1 script line in root) so blast radius on root build is zero by construction.

[2026-04-28T20:05:00Z] Subtask S3 — PASS (after fix) — agent: developer. Fix added per-language `phraseSourceFile`, `phraseSourceExport`, `dailySourceExport` to dataForLanguages.cjs and updated createLevel.cjs to consult these. Smoke results: `--lang de` exit 0; `--lang ru` (with ru.levelTarget=null override) loads 14836 phrases from newCitats.js, generates 20 location levels, exit 0; `git status src/` clean; `node -e "require('./creatingLevels/createLevel.cjs')"` clean. Final `git diff --stat HEAD` shows only `creatingLevels/createLevel.cjs` + 2 legacy deprecation comments. Algorithmic note: location levels use complexity=3 (matching unified main loop), not legacy ru's complexity=4 — judgment call within "use main's algorithm" decision. Did NOT exercise heavy `--lang ru --update` (4000 levels) — the legacy `while(allLevels.length !== untill)` loop only grows, so meaningful timing test would need a fresh DELETE_ALL_BEFORE run; out of scope for S3 acceptance per spec.
