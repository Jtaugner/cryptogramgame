{
  "name": "cryptogram",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "PLATFORM=yandex vite",
    "dev:yt": "PLATFORM=yt vite",
    "dev:screen": "PLATFORM=mobile MODE=screen vite",
    "build:yandex": "PLATFORM=yandex sh -c 'tsc && rm -rf dist && vite build && npm run archive'",
    "build:gp": "PLATFORM=gp sh -c 'tsc && rm -rf dist && vite build && npm run archive'",
    "build:yt": "PLATFORM=yt sh -c 'tsc && rm -rf dist && vite build && npm run archive'",
    "build:gd": "PLATFORM=gd sh -c 'tsc && rm -rf dist && vite build && npm run archive'",
    "build:mobile": "PLATFORM=mobile sh -c 'tsc && rm -rf dist && vite build && npm run archive'",
    "build:screen": "PLATFORM=mobile MODE=screen sh -c 'tsc && rm -rf dist && vite build && npm run archive'",
    "archive": "rm -rf archiveBuildCryptogram && mkdir -p archiveBuildCryptogram && cd dist && zip -r ../archiveBuildCryptogram/dist.zip .",
    "lint": "eslint . --ext ts,tsx --report-unused-disable-directives --max-warnings 0",
    "preview": "vite preview"
  },
  "dependencies": {
    "confetti-react": "^2.6.0",
    "framer-motion": "^12.16.0",
    "i18next": "^25.3.2",
    "lottie-web": "^5.12.2",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-i18next": "^15.6.0",
    "react-lottie-player": "^2.1.0",
    "react-swipeable": "^7.0.2",
    "react-transition-group": "^4.4.5"
  },
  "devDependencies": {
    "@types/react": "^18.2.66",
    "@types/react-dom": "^18.2.22",
    "@typescript-eslint/eslint-plugin": "^7.2.0",
    "@typescript-eslint/parser": "^7.2.0",
    "@vitejs/plugin-react": "^4.2.1",
    "autoprefixer": "^10.4.19",
    "eslint": "^8.57.0",
    "eslint-plugin-react-hooks": "^4.6.0",
    "eslint-plugin-react-refresh": "^0.4.6",
    "postcss": "^8.4.38",
    "tailwindcss": "^3.4.1",
    "typescript": "^5.2.2",
    "vite": "^4.5.3",
    "vite-plugin-html": "^3.2.2"
  }
}
